<svg xmlns="http://www.w3.org/2000/svg" width="150" height="150" viewBox="0 0 150 150">
  <defs>
    <style>
      @keyframes float {
        0% { transform: translateY(0); }
        50% { transform: translateY(-5px); }
        100% { transform: translateY(0); }
      }
      .profile-bg { fill: #2ecc71; }
      .profile-circle { fill: #fff; }
      .profile-details { fill: #2c3e50; }
      .profile-img { animation: float 3s ease-in-out infinite; }
    </style>
  </defs>
  
  <g class="profile-img">
    <!-- Background Circle -->
    <circle class="profile-bg" cx="75" cy="75" r="75"/>
    
    <!-- Profile Circle -->
    <circle class="profile-circle" cx="75" cy="65" r="30"/>
    
    <!-- Body Shape -->
    <path class="profile-circle" d="M 30 140 Q 75 110 120 140" />
    
    <!-- Leaf Icon (for Environmental Specialist) -->
    <path class="profile-details" d="M 60 50 Q 75 30 90 50 Q 75 60 60 50" fill="#2c3e50"/>
    
    <!-- Decorative Elements -->
    <circle class="profile-details" cx="65" cy="60" r="2"/>
    <circle class="profile-details" cx="85" cy="60" r="2"/>
    
    <!-- Hair -->
    <path class="profile-details" d="M 60 45 Q 75 35 90 45" fill="none" stroke="#2c3e50" stroke-width="2"/>
  </g>
</svg>